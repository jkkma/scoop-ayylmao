{
    "version": "0.10.0",
    "description": "Internet radio player and alarm clock - wakes you to a station or to a random track from a folder, with a Frutiger Aero face and a low-poly orb",
    "homepage": "https://github.com/jkkma/aerowave",
    "license": {
        "identifier": "MIT",
        "url": "https://github.com/jkkma/aerowave/blob/main/LICENSE"
    },
    "notes": [
        "Settings live in 'data' inside the app directory, which is persisted, so they survive an update. Nothing is written to your user profile.",
        "Alarms only ring while Aerowave is running. Closing the window hides it to the tray and leaves the clock going; turn on 'Start with Windows' in SETUP if you want it back after a reboot. That registry entry is the only thing it writes outside its own folder.",
        "Set a backup folder in SETUP: it is what rings when a station 404s or will not start.",
        "Needs the WebView2 runtime, which ships with Windows 11 and current Windows 10.",
        "The binary is unsigned: SmartScreen warns the first time a manually downloaded copy runs. Scoop checks the hash for you."
    ],
    "architecture": {
        "64bit": {
            "url": "https://github.com/jkkma/aerowave/releases/download/v0.10.0/Aerowave-0.10.0-win-x64.zip",
            "hash": "a609a1b0b90d670ab77b6ed1b1a06024f291f870ff4bc44c8bf8e06095bf22cc"
        }
    },
    "extract_dir": "Aerowave",
    "bin": "aerowave.exe",
    "shortcuts": [
        [
            "aerowave.exe",
            "Aerowave"
        ]
    ],
    "persist": [
        "data"
    ],
    "checkver": {
        "url": "https://api.github.com/repos/jkkma/aerowave/releases?per_page=100",
        "script": [
            "$releases = ($page | ConvertFrom-Json) | Where-Object {",
            "    !$_.draft -and !$_.prerelease -and $_.tag_name -cmatch '^v\\d+\\.\\d+\\.\\d+$' -and",
            "    $_.assets.name -ccontains ('Aerowave-{0}-win-x64.zip' -f $_.tag_name.Substring(1))",
            "}",
            "$release = $releases | Sort-Object { [version]$_.tag_name.Substring(1) } -Descending | Select-Object -First 1",
            "if (!$release) { throw 'No stable Windows x64 release was found.' }",
            "$release.tag_name"
        ],
        "regex": "^v(\\d+\\.\\d+\\.\\d+)$"
    },
    "autoupdate": {
        "architecture": {
            "64bit": {
                "url": "https://github.com/jkkma/aerowave/releases/download/v$version/Aerowave-$version-win-x64.zip"
            }
        }
    }
}
